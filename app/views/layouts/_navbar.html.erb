<nav class='navbar navbar-fixed-top navbar-inverse'>
  
  <div class='container-fluid'>
  
    <!-- Brand and toggle get grouped for better mobile display -->
		<div class='navbar-header'>
      <button type='button' class='navbar-toggle collapsed' data-toggle='collapse' data-target='#bs-example-navbar-collapse-1' aria-expanded='false'>
        <span class='sr-only'>Toggle navigation</span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
        <span class='icon-bar'></span>
      </button>
      <span class='navbar-brand'>Astronogeek</span>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
		<div class='collapse navbar-collapse' id='bs-example-navbar-collapse-1'>
      <ul class='nav navbar-nav'>
        <li><%= link_to 'Accueil', root_path %></li>
        <li><%= link_to 'Dernières idées', newest_path %></li>
        <% if logged_in? %>
          <li><%= link_to 'Nouvelle idée', new_idea_path %></li>
        <% end %>
      </ul>

      <ul class='nav navbar-nav navbar-right'>
        <% unless logged_in? %>
          <%= link_to 'S\'enregistrer ', new_user_path %>/
          <%= link_to 'Se connecter', new_session_path %>
        <% end %>
        <% if logged_in? %>
          <li><%= link_to'<span class="glyphicon glyphicon-log-out"></span>'.html_safe, sessions_path, method: :delete %></li>
        <% end %>
      </ul>
    </div>
    <!-- /.navbar-collapse -->

  </div>

</nav>
